import React from 'react';
import { Login } from '../components';

const LoginPage = () => {
  return (
    <main>
      <h1>Login Page</h1>
      <Login />
    </main>
  );
};

export default LoginPage;
